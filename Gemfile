source 'http://rubygems.org'

gem 'discogs-wrapper'
gem 'json'
gem 'rack'
gem 'rack-contrib'
gem 'rake'
gem 'rubysl-base64'
gem 'shopify_api'
gem 'sinatra'

# These are the dependencies that are used only for unit tests.
group :test do
  gem 'rack-test'
  gem 'rspec'
end
